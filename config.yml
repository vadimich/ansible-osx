---
cache_dir: ~/Library/Caches/battleschool

sources:
  #local:
  #  - homebrew_playbook.yml

  url:
    - name: Install all the apps via Homebrew and Homebrew Cask
      url: https://raw.githubusercontent.com/vadimich/ansible-osx/master/homebrew_playbook.yml
      
    - name: managing Dock for OSX.yml
      url: https://raw.githubusercontent.com/vadimich/ansible-osx/master/dock_playbook.yml

  git:
    - name: 'osx'
      repo: 'https://github.com/vadimich/ansible-osx'
      playbooks:
         - homebrew.yml                 #must come before any use of homebrew module
